@* @model GCTL.Core.ViewModels.MasterSetup.EmploymentNature.EmploymentNatureVM

<div class="card">
    <div class="card-header border-bottom-1">
        <div class="row justify-content-between align-items-center">
            <div class="col-auto">
                <h3 class="text-body-emphasis mt-3">@ViewBag.AddEmploymentNature</h3>
            </div>
        </div>
    </div>

    <div class="card-body mt-3">
        <form asp-action="Create" asp-controller="EmploymentNatures" method="post" enctype="multipart/form-data" id="employmentNature-form">
            @Html.AntiForgeryToken()
            <div class="col-sm-12 col-md-12 mb-4">
                <input type="hidden" asp-for="EmploymentNatureID" />

                <div class="flatpickr-input-container">
                    <div class="form-floating">
                        <input class="form-control" asp-for="EmploymentNatureName" type="text" placeholder="Employment Nature Name" autocomplete="off" />
                        <label asp-for="EmploymentNatureName">@ViewBag.EmploymentNatureName <span style="color: red;">*</span></label>
                    </div>
                    <span asp-validation-for="EmploymentNatureName" class="text-danger"></span>
                    <span id="nameError" class="text-danger" style="display:none;"></span>
                </div>
            </div>

            <div class="col-12 mb-4">
                <div class="row g-3 justify-content-end">
                    <div class="col-auto">
                        <button type="reset" class="btn btn-phoenix-primary px-5" id="employmentNature-resetBtn">@ViewBag.Reset</button>
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary px-5 px-sm-15" id="employmentNature-saveBtn">@ViewBag.Save</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div> *@
@model GCTL.Core.ViewModels.MasterSetup.EmploymentNature.EmploymentNatureVM

<div class="card">
    <div class="card-header border-bottom-1">
        <div class="row justify-content-between align-items-center">
            <div class="col-auto">
                <h3 class="text-body-emphasis mt-3">@Html.SmartLocalize("Add Employment Nature")</h3>
            </div>
        </div>
    </div>

    <div class="card-body mt-3">
        <form asp-action="Create" asp-controller="EmploymentNatures" method="post" enctype="multipart/form-data" id="employmentNature-form">
            @Html.AntiForgeryToken()
            <div class="col-sm-12 col-md-12 mb-4">
                <input type="hidden" asp-for="EmploymentNatureID" />

                <div class="flatpickr-input-container">
                    <div class="form-floating">
                        <input class="form-control" asp-for="EmploymentNatureName" type="text" placeholder="@Html.SmartLocalize("Employment Nature Name")" autocomplete="off" />
                        <label asp-for="EmploymentNatureName">@Html.SmartLocalize("Employment Nature Name") <span style="color: red;">*</span></label>
                    </div>
                    <span asp-validation-for="EmploymentNatureName" class="text-danger"></span>
                    <span id="nameError" class="text-danger" style="display:none;"></span>
                </div>
            </div>

            <div class="col-12 mb-4">
                <div class="row g-3 justify-content-end">
                    <div class="col-auto">
                        <button type="reset" class="btn btn-phoenix-primary px-5" id="employmentNature-resetBtn">@Html.SmartLocalize("Reset")</button>
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary px-5 px-sm-15" id="employmentNature-saveBtn">@Html.SmartLocalize("Save")</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
