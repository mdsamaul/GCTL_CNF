@{
    var mainTab = "CompanySetting";
    var subTab = "DesignationSettings";
}

<partial name="~/Views/Shared/AdminsettingsNavs/CompanySettingsNavs/_MainSettingsTabPartial.cshtml" model="mainTab" />
<partial name="~/Views/Shared/AdminsettingsNavs/CompanySettingsNavs/_NavCompanySettingsPartial.cshtml" model="subTab" />


<div class="col-xl-12 mt-3">
    <div class="card mb-4">
        <div class="card-body">
            <div class="border-bottom d-flex align-items-center justify-content-between pb-3 mb-3">
                <h4>Designation Settings</h4>

            </div>


            <form id="designationSettingsForm" asp-controller="DesignationSettings" asp-action="Create" method="post">
                <div class="mb-3">
                    <div class="row">
                        <div class="col-md-12">
                            <div>
                                <h6 class="mb-3">Basic Information</h6>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-4">
                            <div class="mb-3">
                                <label class="form-label" for="OrganizationID">Organization Name</label>
                                <select id="OrganizationID" name="OrganizationID" class="form-select choiceDD">
                                    <option value="">-- Select Organization --</option>
                                    @if (ViewBag.Organizations != null)
                                    {
                                        foreach (var org in ViewBag.Organizations)
                                        {
                                            <option value="@org.Value">@org.Text</option>
                                        }
                                    }
                                </select>
                            </div>
                        </div>

                        <div class="col-md-4">
                            <div class="mb-3">
                                <label class="form-label" for="DesignationName">Designation Name</label>
                                <input class="form-control" id="DesignationName" name="DesignationName" type="text" placeholder="Branch Name" />
                            </div>
                        </div>

                    </div>
                </div>


                <div class="d-flex align-items-center justify-content-end">
                    <button type="button" class="btn btn-outline-secondary border me-3">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
  
   
</div>


<partial name="_ConfirmDelete" />
<partial name="_DesignationSettingTable"/>
@section Scripts {
    <script src="~/js/pages/adminsettings/organizationsettings/designationsetting.js"></script>

}